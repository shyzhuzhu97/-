<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edward.edu.mapper.EduTeacherMapper">
    <select id="findTeacherAll" resultType="com.edward.edu.bean.EduTeacher">
        SELECT *FROM edu_teacher WHERE is_deleted = 0
    </select>
    <select id="findTeacherById" parameterType="int" resultType="com.edward.edu.bean.EduTeacher">
        SELECT *FROM edu_teacher WHERE id = #{teacherId} AND is_deleted = 0
    </select>
    <select id="findFourTeacher" resultType="com.edward.edu.bean.EduTeacher">
       SELECT *FROM edu_teacher WHERE is_deleted=0 ORDER BY RAND() LIMIT 4
    </select>
    <insert id="addTeacher" parameterType="com.edward.edu.bean.EduTeacher" >
        INSERT INTO `edu_manager`.`edu_teacher` (`name`, `sort`, `level`, `career`, `intro`, `avatar`, `is_deleted`, `gmt_created`, `gmt_modified`) VALUES (#{name}, #{sort},#{level}, #{career}, #{intro}, #{avatar}, #{isDeleted}, #{gmtCreated}, #{gmtModified});
    </insert>
</mapper>